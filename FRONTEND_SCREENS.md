# 프론트엔드 화면 기획안 (GAME_RULES 기준)

이 문서는 [GAME_RULES.md](./GAME_RULES.md)의 규칙에 맞춰 1단계/2단계/결과 화면이 갖춰야 할 요소를 정리한 체크리스트입니다. 구현 시 이 목록을 기준으로 검증하세요.

---

## 기준 문서

- **규칙 정의**: [GAME_RULES.md](./GAME_RULES.md) — 용어, 경매 룰, 아이템, 상태 설계
- **구현 위치**: `JoopJoop_front/src/app/components/` — HomeScreen, RoomSelectionScreen, HelpScreen, LobbyScreen, ItemSelectionScreen, Phase1Screen, Phase2Screen, ResultScreen

---

## 진입·로비 흐름

| 화면 | 컴포넌트 | UI / 동작 |
|------|-----------|-----------|
| **초기 로비** | `HomeScreen` | 닉네임 입력, **입장하기**만. 입장 시 `room_selection`으로 이동. |
| **방 참가 메뉴** | `RoomSelectionScreen` | **방 만들기** → `create_room` → 게임 대기(`LobbyScreen`). **코드로 입장하기** → 팝업에서 코드 입력 후 `join_room` → 게임 대기. **빠른 참가** → 인원(3~6) 선택 팝업 → 확인 후 `join_match_queue` → 대기 중 오버레이(스피너·매칭 취소). 인원이 모이면 서버가 `item_selection`부터 시작 → **게임 대기 생략**. **도움말** → `HelpScreen`(다단계 페이지). |
| **도움말** | `HelpScreen` | 목표 → 1단계 → 2단계 → 아이템, 이전/다음·인디케이터·마지막에 닫기. |
| **게임 대기** | `LobbyScreen` | 방 코드, 참가자, 채팅, 준비/시작(기존과 동일). |

팝업은 메인 카드 위 반투명 백드롭(`z-40`)으로 덮어 시각적 계층을 구분한다. 토스트 에러는 더 높은 `z-index`를 사용한다.

**나가기 / 결과 돌아가기**: `App.tsx`의 `leaveToRoomMenu`. **`room:destroyed`(방 파괴)**: `handleRoomDestroyed`에서 `applyRoomMenuOnly`로 **`room_selection`** 복귀 후, 상단 **배너**로만 안내한다(`window.alert` 미사용 — 알림 닫기 클릭이 뒤 UI로 전파되는 문제 방지). **닉네임은 유지**. 초기 로비(`home`)는 방 참가 메뉴에서 뒤로(로그아웃, `resetToHome`)일 때만.

---

## 화면·디바이스 기준

- **개발 기준 기기**: **iPhone 13 mini**
- 화면 배치 및 요소 크기는 **iPhone 13 mini** 화면에서 볼 때 **매끄럽게** 보이도록 개발한다.
- 참고: iPhone 13 mini 뷰포트(포트레이트) — **375 × 812 pt** (CSS 미디어 쿼리·레이아웃 검증 시 이 크기를 기준으로 한다).
- 더 큰 기기에서도 동작하되, 우선 목표는 375pt 너비 기준으로 스크롤·겹침·잘림 없이 자연스럽게 보이는 것이다.

---

## 1단계: 직업 카드 경매 화면 (Phase1Screen)

| # | GAME_RULES 요구사항 | 구현 여부 | 비고 |
|---|---------------------|-----------|------|
| 1 | 경매 라운드 정보 (현재 라운드, 전체 가능 라운드 수) | ✅ | `gameState.roundNumber` 표기 |
| 2 | 각 유저가 베팅을 결정하는 남은 시간 (10초) | ✅ | `useTimeLeft`, 턴 타이머 표시 |
| 3 | 다른 참가자 프로필 사진, 닉네임 | ✅ | opponents 영역 |
| 4 | 다른 참가자들이 베팅한 금액 | ✅ | `player.currentBid` 표시 |
| 5 | 베팅 포기한 참가자 표시 | ✅ | "포기" 뱃지, 회색 처리 |
| 6 | 베팅 순서 표시 | ⚠️ | 턴 순서는 턴 강조로 간접 표현. 순서 번호(1,2,3,4) 표기 권장 |
| 7 | 현재 베팅 순서인 사람 표시 | ✅ | "고민 중..." + ring 강조 |
| 8 | 펼쳐진 직업 카드 목록 (참여자 수만큼) | ✅ | `gameState.currentProperties` |
| 9 | 아직 펼쳐지지 않은 직업 카드 뭉치 (뒷면) | ✅ | "직업 카드 더미" 덱 |
| 10 | 내 프로필 사진 | ✅ | 하단 내 정보 영역 |
| 11 | 내가 가진 남은 돈 | ✅ | `me?.coins` |
| 12 | 내가 획득한 직업 카드 목록 | ✅ | `me?.properties` 미니 카드 |
| 13 | 베팅하기 버튼 | ✅ | 1,000원 단위 증감 포함 |
| 14 | 베팅 포기 버튼 | ✅ | 리버스 시 "포기 불가!" 처리 |
| 15 | 베팅 금액 설정 버튼 (+/-) | ✅ | adjustBid ±1000 |
| — | 아이템 사용 (리롤/엿보기/리버스) | ✅ | 내 아이템 패널, 턴/라운드 조건 반영 |
| — | 다른 참가자 아이템 표시 (선택한 아이템 공개) | ✅ | 프로필 하단 아이콘 |
| — | 리버스 시 역방향 진행 표시 | ✅ | "↩ 역방향 진행중!" 뱃지 |

**보완 권장**

- 베팅 **순서 번호**(예: 1번, 2번, 3번)를 참가자 카드에 명시하면 규칙과의 대응이 더 분명해짐.

---

## 2단계: 부동산 카드 경매 화면 (Phase2Screen)

| # | GAME_RULES 요구사항 | 구현 여부 | 비고 |
|---|---------------------|-----------|------|
| 1 | 참가자들의 프로필, 이름 | ✅ | opponents 영역 |
| 2 | 참가자들이 제출한 카드 여부 (제출 전/후) | ✅ | "제출 완료" / "고민 중..." |
| 3 | 카드 제출에 남은 시간 | ✅ | `useTimeLeft`, phase2StartTime |
| 4 | 라운드 숫자 텍스트 | ✅ | `gameState.phase2RoundNumber` |
| 5 | 참가자 수에 맞게 펼쳐진 부동산 카드들 | ✅ | `gameState.currentRealEstateCards` |
| 6 | 아직 펼쳐지지 않은 부동산 카드 뭉치 | ✅ | "부동산 카드" 덱 |
| 7 | 내 프로필 | ✅ | 하단 내 정보 |
| 8 | 내 남은 돈 | ✅ | `me?.coins` |
| 9 | 내가 가진 직업 카드들 (선택 가능) | ✅ | JobCardMini 클릭 제출 |
| 10 | 내가 획득한 부동산 카드 목록 | ✅ | "내 부동산" 영역 |
| — | 아이템 사용 (리롤/엿보기) | ✅ | 2단계는 리버스 미지원 |
| — | 제출 후 카드 동시 오픈 연출 | ✅ | `allPlayersSelected` 시 카드 공개 |

**규칙 준수**

- 제한 시간 내 미선택 시 **가장 낮은 직업 카드 자동 제출** — 서버 처리 필요, 프론트는 타이머 표시만.
- 부동산 카드 배분(높은 직업 → 높은 부동산) 및 동점 시 턴 순서 — 서버 로직, 프론트는 결과 반영만.

---

## 결과 화면 (ResultScreen)

| # | GAME_RULES 요구사항 | 구현 여부 | 비고 |
|---|---------------------|-----------|------|
| 1 | 참가자 닉네임, 프로필 | ✅ | rankings 기반 렌더 |
| 2 | 획득한 부동산 카드 목록 및 총합 | ✅ | realEstateCards + estateValue |
| 3 | 남은 돈 | ✅ | remainingCoins |
| 4 | 최종 점수 = 부동산 총액 + 남은 돈 | ✅ | finalScore |
| 5 | 순위 (1등, 2등, …) 표시 | ✅ | rank, MedalIcon, "우승!" |

**규칙 준수**

- GAME_RULES § 결과 화면: "부동산 총액 + 남은 돈" 합산 순위 — 현재 `PlayerRanking` 구조와 일치.
- 하단 버튼: `continueLabel` + `onContinue` — 비공개 방은 **계속하기**(`return_to_lobby`), 빠른 참가는 **다시 매칭하기**(동일 인원 큐 재진입).

---

## 화면 전환 (App.tsx)

- **home**(닉네임) → **room_selection**(방 참가 메뉴) → **lobby**(게임 대기) 또는 **help** 또는 매칭 완료 시 곧바로 **item_selection**
- 게임 대기에서 방장 시작 → **item_selection** → **1단계**(`game` / `phase1_bidding`) → **2단계**(`phase2_playing`) → **결과**(`game_over`)
- **결과**에서 **계속하기**: 방 만들기·코드 입장으로 들어온 경우 `return_to_lobby`로 같은 방 **lobby** 복귀 / **빠른 참가**로 들어온 경우 방 이탈 후 **동일 인원**으로 매칭 큐 재진입(`다시 매칭하기`).
- 서버 `phase` 우선순위: `game_over` → `item_selection` → `lobby` → 그 외 `gameState === 'playing'`(1·2단계).
- 로비·게임·결과에서 **나가기** 또는 **방 파괴** 시 `room_selection`으로 복귀(닉네임 유지). **`home`**(닉네임 재입력)은 방 참가 메뉴에서 뒤로가기(`resetToHome`)일 때만.

---

## 공통 확인 사항

1. **타이머**: 서버 기준 시간(`turnStartTime`, `phase2StartTime`)으로 남은 시간 계산. 만료 시 서버가 자동 포기/자동 카드 선택 처리.
2. **비공개 정보**: 다른 참가자의 남은 돈·직업 카드는 노출하지 않음. 엿보기 사용 시에만 일회성으로 표시.
3. **아이템 공개**: 누가 어떤 아이템을 선택했는지는 모든 참가자에게 보이도록 구현됨 (Phase1/Phase2 참가자 카드에 아이콘).

이 문서는 [GAME_RULES.md](./GAME_RULES.md)가 수정되면 해당 섹션(§6 화면/UX 요구사항)과 함께 갱신해야 합니다.
